/**
 * Enid, OK jobs — generated from the templating system.
 * To modify: edit `lib/data/configs/enid.ts`
 */

import { buildJobs } from "../build-jobs";
import { ENID_CONFIG } from "../configs/enid";

export const ENID_JOBS = buildJobs(ENID_CONFIG);
