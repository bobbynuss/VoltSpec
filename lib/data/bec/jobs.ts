/**
 * BEC (Bandera Electric) jobs - generated from the templating system.
 * To modify: edit `lib/data/configs/bec.ts`
 */

import { buildJobs } from "../build-jobs";
import { BEC_CONFIG } from "../configs/bec";

export const BEC_JOBS = buildJobs(BEC_CONFIG);
