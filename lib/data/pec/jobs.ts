/**
 * PEC (Hill Country) jobs - generated from the templating system.
 * To modify: edit `lib/data/configs/pec.ts`
 */

import { buildJobs } from "../build-jobs";
import { PEC_CONFIG } from "../configs/pec";

export const PEC_JOBS = buildJobs(PEC_CONFIG);
