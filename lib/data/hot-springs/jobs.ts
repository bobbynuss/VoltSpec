/**
 * Hot Springs, AR jobs — generated from the templating system.
 * To modify: edit `lib/data/configs/hot-springs.ts`
 */

import { buildJobs } from "../build-jobs";
import { HOT_SPRINGS_CONFIG } from "../configs/hot-springs";

export const HOT_SPRINGS_JOBS = buildJobs(HOT_SPRINGS_CONFIG);
