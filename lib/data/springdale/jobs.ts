/**
 * Springdale/NW Arkansas jobs — generated from the templating system.
 * To modify: edit `lib/data/configs/springdale.ts`
 */

import { buildJobs } from "../build-jobs";
import { SPRINGDALE_CONFIG } from "../configs/springdale";

export const SPRINGDALE_JOBS = buildJobs(SPRINGDALE_CONFIG);
