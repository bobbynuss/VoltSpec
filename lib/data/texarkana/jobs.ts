/**
 * Texarkana, TX jobs — generated from the templating system.
 * To modify: edit `lib/data/configs/texarkana.ts`
 */

import { buildJobs } from "../build-jobs";
import { TEXARKANA_CONFIG } from "../configs/texarkana";

export const TEXARKANA_JOBS = buildJobs(TEXARKANA_CONFIG);
