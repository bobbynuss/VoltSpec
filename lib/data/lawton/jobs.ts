/**
 * Lawton, OK jobs — generated from the templating system.
 * To modify: edit `lib/data/configs/lawton.ts`
 */

import { buildJobs } from "../build-jobs";
import { LAWTON_CONFIG } from "../configs/lawton";

export const LAWTON_JOBS = buildJobs(LAWTON_CONFIG);
