/**
 * Lufkin/East TX jobs - generated from the templating system.
 * To modify: edit `lib/data/configs/lufkin.ts`
 */

import { buildJobs } from "../build-jobs";
import { LUFKIN_CONFIG } from "../configs/lufkin";

export const LUFKIN_JOBS = buildJobs(LUFKIN_CONFIG);
